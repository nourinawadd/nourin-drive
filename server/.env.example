PORT=5000
NODE_ENV=development

CLIENT_URL=http://localhost:3000
BLOG_URL=http://localhost:4000
# ALLOWED_ORIGINS=https://nourin.is-a.dev,https://blog.nourin.is-a.dev

# Optional in Section 1 — server will boot without it.
# Required from Section 6 onwards (guestbook + blog need persistence).
MONGO_URI=mongodb://localhost:27017/nourin

# Optional admin guard for destructive routes (added in Section 6).
ADMIN_TOKEN=
